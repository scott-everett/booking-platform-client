import { RegisterAccountRequest } from './register-account-request';

describe('RegisterAccountRequest', () => {
  it('should create an instance', () => {
    expect(new RegisterAccountRequest()).toBeTruthy();
  });
});
