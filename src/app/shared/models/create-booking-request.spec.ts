import { CreateBookingRequest } from './create-booking-request';

describe('CreateBookingRequest', () => {
  it('should create an instance', () => {
    expect(new CreateBookingRequest()).toBeTruthy();
  });
});
