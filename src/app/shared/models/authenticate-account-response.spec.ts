import { AuthenticateAccountResponse } from './authenticate-account-response';

describe('AuthenticateAccountResponse', () => {
  it('should create an instance', () => {
    expect(new AuthenticateAccountResponse()).toBeTruthy();
  });
});
