import { AuthenticateAccountRequest } from './authenticate-account-request';

describe('AuthenticateAccountRequest', () => {
  it('should create an instance', () => {
    expect(new AuthenticateAccountRequest()).toBeTruthy();
  });
});
